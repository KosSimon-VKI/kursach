<Window x:Class="Агеенков_курсач.Admin.Пользователи.ManageUsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление пользователями" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Пользователи" FontSize="16" FontWeight="Bold" Grid.Row="0"/>

        <DataGrid x:Name="UsersGrid" Grid.Row="1" Margin="0,10"
                 AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="Auto"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding логин}" Width="*"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding фио}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Тип}" Width="*"/>
                <DataGridTextColumn Header="Проект" Binding="{Binding Проект}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="*"/>
                <DataGridTemplateColumn Header="Действия" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Изменить" Margin="2" Click="EditUser_Click"/>
                                <Button Content="Удалить" Margin="2" Click="DeleteUser_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Width="80" Margin="0,0,10,0" Click="AddUser_Click"/>
            <Button Content="Назад" Width="80" Click="BackButton_Click"/>
        </StackPanel>
    </Grid>
</Window>